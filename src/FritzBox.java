/**
 * The class {@code FritzBox} represents a Fritz!Box.
 * 
 * @author moritz
 * 
 */
public class FritzBox {

    /**
     * Tries to wake up the device with mac-address mac.
     * 
     * @param mac
     *            the specified mac-address
     * @return returns true if the operation succeeded
     */
    public boolean wakeUp(String mac) {
	boolean success = false;

	return success;
    }

    /**
     * Tries to restart the Fritz!Box
     * 
     * @return returns true if the operation succeeded
     */
    public boolean restart() {
	boolean success = false;

	return success;
    }
}
